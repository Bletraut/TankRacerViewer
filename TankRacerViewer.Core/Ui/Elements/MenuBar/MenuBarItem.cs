using ComposableUi;

namespace TankRacerViewer.Core
{
    public sealed class MenuBarItem : ButtonElement
    {
        public MenuBarItem() 
        { 
        }

        protected override void OnInteractionStateChanged(InteractionState state)
        {
            base.OnInteractionStateChanged(state);
        }
    }
}
